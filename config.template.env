# === Core speech settings ===

# Path to your Vosk model folder
# If empty, YoChan will try to auto-detect from ./models
MODEL_PATH=vosk_models/vosk-model-small-en-us-0.15

# Path to Porcupine wake-word .ppn file or folder containing .ppn files
# If empty, YoChan will try to auto-detect from ./porcupine_models
WAKE_WORD_PATH=porcupine_models/Yo-Chan_en_linux_v3_0_0.ppn

# Your Picovoice access key (required for Porcupine)
ACCESS_KEY=picovoice_pinecone_key_goes_here

# How long (in seconds) YoChan listens after the wake word
LISTEN_DURATION=5


# === Assistant behaviour & tuning ===

# 0.0–1.0: how strict fuzzy matching is for app names
# Lower = more forgiving, higher = more exact
FUZZY_THRESHOLD=0.7

# Optional: override assistant name shown in notifications / configurator
# If empty, it is derived from the .ppn filename (e.g. "Yo-Chan")
ASSISTANT_NAME=Yo-Chan


# === Power / system commands (optional overrides) ===
# Leave empty to use sane defaults from config.py based on your desktop.:contentReference[oaicite:2]{index=2}
# You can override with any shell command line.

SHUTDOWN_COMMAND=
REBOOT_COMMAND=
SUSPEND_COMMAND=
LOGOUT_COMMAND=
